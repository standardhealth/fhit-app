import type { NextPage } from 'next';
import { Typography } from '@mui/material';

const Home: NextPage = () => {
  return (
    <div>
      <Typography variant="h1">Hello, I am FHIT.*</Typography>
      <Typography variant="caption">*coming soon</Typography>
    </div>
  );
};

export default Home;
